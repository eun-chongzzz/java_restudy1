package day4;

// class Animal{
	
// }

public class Sample {

	public static void main(String[] args) {
		// 이번 장에서는 클래스에 대해서 자세히 알아보자
		// 여기서는 클래스에 대한 개념적인 설명을 하기보다는 가장 간단한 클래스를 작성하는것부터
		// 시작해서 조금씩 클래스의 규모를 키워가는 방식으로 클래스를 설명해 볼까 한다.
		
		// 위 Animail 클래스는 가장 간단한 형태의 클래스이다.
		// 클래스의 선언만 있고 내용이 없는 껍데기뿐인 클래스이다.
		// 하지만 이 껍데기 분인 클래스도 아주 중요한 기능을 가지고있따.
		// 그 기능은 바로 객체(object)를 만드는 기능이다.
		
		// 객체는 다음과 같이 만들 수 있따.
		
		// Animal cat = new Animal();
		
		// new는 객체를 생성할 때 사용하는 키워드이다.
		// 이렇게 하면 Animail 클래스의 인스턴스(instance)인 cat, 즉 Animail의 객체가 만들어진다.
		
		// ※ 클래스에 의해서 만들어진 객체를 인스턴스라고도 한다. 그렇다면 객체와 인스턴스의 차이는 무엇일까?
		
		// 이렇게 생각해보자. Animal cat = new Animal() 이렇게 만들어진 cat은 객체이다.
		// 그리고 cat이라는 객체는 Animal의 인스턴스(instance)이다
		// 인스턴스라는 말은 특정 객체가 어떤 클래스의 객체 인지를 관계위주로 설명할때 사용된다.
		// 즉, "cat은 인스턴스" 보다는  "cat은 객체"라는 표현이 
		// "cat은 Animal의 객체" 보다는 "cat은 Animal의 인스턴스"라는 표현이 훨씬 잘 어울린다
		
		//Animal dog = new Animal();
		//Animal horse = new Animal();
		
	}

}
